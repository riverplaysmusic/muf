<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/png" sizes="32x32" href="/favicon/favicon-32x32.png" />
		<link rel="icon" type="image/png" sizes="192x192" href="/favicon/icon-192.png" />
		<link rel="apple-touch-icon" href="/favicon/apple-touch-icon.png" />
		<link rel="manifest" href="/manifest.json" />
		<meta name="theme-color" content="#000000" />
		<meta name="apple-mobile-web-app-capable" content="yes" />
		<meta name="apple-mobile-web-app-status-bar-style" content="black-translucent" />
		<meta name="apple-mobile-web-app-title" content="Musical Universe Factory" />
		<meta name="generator" content={Astro.generator} />
		<title>Musical Universe Factory</title>
	</head>
	<body>
		<slot />
	</body>
</html>

<style is:global>
	:root {
		--c-bg: #000000;
		--c-text: #ffffff;
		--c-neon: #0ff; /* Cyan default, can vary */
		--ease-smooth: cubic-bezier(0.25, 0.46, 0.45, 0.94);
	}

	html,
	body {
		margin: 0;
		width: 100%;
		height: 100%;
		background-color: var(--c-bg);
		color: var(--c-text);
		font-family: system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
		-webkit-font-smoothing: antialiased;
		overflow-x: hidden;
	}

	/* Typography */
	h1, h2, h3, h4, h5, h6 {
		font-weight: 800;
		letter-spacing: -0.02em;
	}

	/* Interactions (300ms smooth) */
	a {
		color: inherit;
		text-decoration: none;
		transition: all 0.3s var(--ease-smooth);
	}

	a:hover {
		color: var(--c-neon);
		text-decoration: underline;
		letter-spacing: 0.1em; /* tracking-widest approx */
		text-shadow: 0 0 8px var(--c-neon);
	}

	img, .icon {
		transition: transform 0.3s var(--ease-smooth), filter 0.3s var(--ease-smooth);
	}

	img:hover, .icon:hover {
		transform: scale(1.05);
		filter: brightness(1.2);
	}
</style>