<footer class="global-footer">
  <div class="footer-content">
    <p class="footer-copyright">&copy; 2026 MUSICAL UNIVERSE FACTORY. ALL RIGHTS RESERVED.</p>
    <nav class="footer-links">
      <a href="/contact" class="footer-link">CONTACT</a>
      <a href="/privacy" class="footer-link">PRIVACY</a>
      <a href="/terms" class="footer-link">TERMS</a>
    </nav>
  </div>
</footer>

<style>
  .global-footer {
    width: 100%;
    margin-top: 0;
    margin-bottom: 0;
    padding: var(--space-2xl) var(--space-md);
    background: oklch(62.3% 0.214 259.815);
  }

  .footer-content {
    display: flex;
    justify-content: space-between;
    align-items: center;
    max-width: 1400px;
    margin: 0 auto;
    gap: var(--space-md);
  }

  .footer-copyright {
    font-size: 0.9rem;
    font-weight: 700;
    letter-spacing: 0.1em;
    color: var(--space-black);
    margin: 0;
  }

  .footer-links {
    display: flex;
    gap: var(--space-md);
    align-items: center;
  }

  .footer-link {
    font-size: 0.9rem;
    font-weight: 700;
    letter-spacing: 0.1em;
    color: var(--space-black);
    text-decoration: none;
    transition: color var(--transition-duration) var(--ease-smooth);
  }

  .footer-link:hover {
    color: var(--primary-yellow);
  }

  @media (max-width: 768px) {
    .footer-content {
      flex-direction: column;
      text-align: center;
      gap: var(--space-sm);
    }

    .footer-copyright {
      font-size: 0.8rem;
      letter-spacing: 0.08em;
    }

    .footer-links {
      gap: var(--space-sm);
    }

    .footer-link {
      font-size: 0.8rem;
    }
  }

  @media (max-width: 640px) {
    .global-footer {
      padding: var(--space-md) var(--space-sm);
    }
  }
</style>
