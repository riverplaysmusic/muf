# Environment-specific configuration overrides
# Copy this file to .env and customize for your environment
# The .env file is automatically loaded by config.sh if it exists

# --- Application Configuration ---
# APP_NAME=musical-universe-factory
# REGION=us-central1

# --- GCP Project Configuration ---
# Required for setup_fresh.sh to create new projects and link billing
# Find your billing account ID: gcloud billing accounts list
# BILLING_ACCOUNT_ID=your-billing-account-id-here

# --- Artifact Registry Configuration ---
# REPO_NAME=muf-repo
# IMAGE_TAG=latest

# --- Cloud Run Configuration ---
# CLOUD_RUN_PORT=8080
# CLOUD_RUN_MIN_INSTANCES=0
# CLOUD_RUN_MAX_INSTANCES=10
# CLOUD_RUN_MEMORY=512Mi
# CLOUD_RUN_CPU=1
# CLOUD_RUN_CONCURRENCY=80
# CLOUD_RUN_TIMEOUT=300
# CLOUD_RUN_ALLOW_UNAUTHENTICATED=true
# CLOUD_RUN_SERVICE_ACCOUNT=

# --- Retry Configuration ---
# RETRY_ATTEMPTS=5
# RETRY_DELAY=60

# ============================================================
# Example Configurations
# ============================================================

# --- Example: Initial Setup (setup_fresh.sh) ---
# Use this when creating a new GCP project from scratch
# BILLING_ACCOUNT_ID=01234A-56789B-CDEF01
# APP_NAME=muf-prod
# REGION=us-central1

# --- Example: Production Environment ---
# Uncomment and customize these for a production deployment:
# APP_NAME=muf-prod
# CLOUD_RUN_MIN_INSTANCES=1
# CLOUD_RUN_MAX_INSTANCES=100
# CLOUD_RUN_MEMORY=1Gi
# CLOUD_RUN_CPU=2
# CLOUD_RUN_ALLOW_UNAUTHENTICATED=false
# CLOUD_RUN_SERVICE_ACCOUNT=muf-prod-sa@project-id.iam.gserviceaccount.com

# --- Example: Development Environment ---
# Uncomment and customize these for a development deployment:
# APP_NAME=muf-dev
# IMAGE_TAG=dev
# CLOUD_RUN_MIN_INSTANCES=0
# CLOUD_RUN_MAX_INSTANCES=5
# CLOUD_RUN_MEMORY=256Mi
